## Fix: canonical — /

**Problem:** Canonical tag points to wrong domain `weststylekft.hu` instead of `weststylee.hu`.
**Where to add it:** Inside `<head>`, replace existing `<link rel="canonical">` tag

```html
<link rel="canonical" href="https://weststylee.hu/" />
```

**Before (current code):**
```html
<link rel="canonical" href="https://weststylekft.hu/" />
```

**GitHub commit message:** `fix(seo): correct canonical domain on homepage`

---

## Fix: title_tag — /

**Problem:** Title is too short and missing primary service keywords.
**Where to add it:** Inside `<head>`, replace existing `<title>` tag

```html
<title>West Style Kft. – Professzionális Takarítás és Facility Management Budapest</title>
```

**Before (current code):**
```html
Check page source
```

**GitHub commit message:** `fix(seo): expand homepage title tag with primary keywords`

---

## Fix: structured_data — /

**Problem:** No structured data present on the homepage.
**Where to add it:** Inside `<head>`, before closing `</head>` tag

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "West Style Kft.",
  "url": "https://weststylee.hu/",
  "logo": "https://weststylee.hu/images/logo.png",
  "description": "West Style Kft. professzionális takarítási és facility management szolgáltatásokat nyújt budapesti és magyarországi vállalkozások számára.",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "HU",
    "addressLocality": "Budapest"
  },
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "customer service",
    "availableLanguage": "Hungarian",
    "url": "https://weststylee.hu/kapcsolat"
  },
  "sameAs": []
}
</script>
```

**Before (current code):**
```html
<!-- No structured data present -->
```

**GitHub commit message:** `feat(seo): add Organization schema to homepage`

---
---

## Fix: canonical — /szolgaltatasok

**Problem:** Canonical tag points to wrong domain `weststylekft.hu` instead of `weststylee.hu`.
**Where to add it:** Inside `<head>`, replace existing `<link rel="canonical">` tag

```html
<link rel="canonical" href="https://weststylee.hu/szolgaltatasok" />
```

**Before (current code):**
```html
<link rel="canonical" href="https://weststylekft.hu/szolgaltatasok" />
```

**GitHub commit message:** `fix(seo): correct canonical domain on szolgaltatasok page`

---

## Fix: meta_description — /szolgaltatasok

**Problem:** Meta description is under 100 characters and lacks specific service detail.
**Where to add it:** Inside `<head>`, replace existing `<meta name="description">` tag

```html
<meta name="description" content="Fedezze fel a West Style Kft. takarítási és facility management szolgáltatásait – irodatakarítás, ipari takarítás, karbantartás Budapesten és környékén." />
```

**Before (current code):**
```html
Check page source
```

**GitHub commit message:** `fix(seo): expand meta description on szolgaltatasok page`

---

## Fix: structured_data — /szolgaltatasok

**Problem:** No structured data present on the services page.
**Where to add it:** Inside `<head>`, before closing `</head>` tag

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "West Style Kft.",
  "url": "https://weststylee.hu/szolgaltatasok",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "HU",
    "addressLocality": "Budapest"
  },
  "hasOfferCatalog": {
    "@type": "OfferCatalog",
    "name": "Szolgáltatások",
    "itemListElement": [
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Irodatakarítás",
          "description": "Professzionális irodatakarítási szolgáltatás budapesti vállalkozások számára."
        }
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Ipari takarítás",
          "description": "Ipari és üzemi takarítási megoldások egyedi igények szerint."
        }
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Facility management",
          "description": "Komplex facility management szolgáltatások épületüzemeltetéshez."
        }
      }
    ]
  }
}
</script>
```

**Before (current code):**
```html
<!-- No structured data present -->
```

**GitHub commit message:** `feat(seo): add LocalBusiness and Service schema to szolgaltatasok page`

---
---

## Fix: canonical — /rolunk

**Problem:** Canonical tag points to wrong domain `weststylekft.hu` instead of `weststylee.hu`.
**Where to add it:** Inside `<head>`, replace existing `<link rel="canonical">` tag

```html
<link rel="canonical" href="https://weststylee.hu/rolunk" />
```

**Before (current code):**
```html
<link rel="canonical" href="https://weststylekft.hu/rolunk" />
```

**GitHub commit message:** `fix(seo): correct canonical domain on rolunk page`

---

## Fix: meta_description — /rolunk

**Problem:** Meta description is critically short at 71 characters, missing company value proposition.
**Where to add it:** Inside `<head>`, replace existing `<meta name="description">` tag

```html
<meta name="description" content="Ismerje meg a West Style Kft. csapatát! Több mint 10 éves tapasztalattal nyújtunk megbízható takarítási és facility management megoldásokat magyarországi ügyfeleknek." />
```

**Before (current code):**
```html
Check page source
```

**GitHub commit message:** `fix(seo): expand meta description on rolunk page`

---

## Fix: title_tag — /rolunk

**Problem:** Title is very short and lacks keyword optimization.
**Where to add it:** Inside `<head>`, replace existing `<title>` tag

```html
<title>Rólunk – West Style Kft. | Megbízható Takarítás és Facility Management</title>
```

**Before (current code):**
```html
Check page source
```

**GitHub commit message:** `fix(seo): expand title tag on rolunk page`

---

## Fix: structured_data — /rolunk

**Problem:** No structured data present on the about page.
**Where to add it:** Inside `<head>`, before closing `</head>` tag

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "West Style Kft.",
  "url": "https://weststylee.hu/rolunk",
  "description": "West Style Kft. több mint 10 éve nyújt professzionális takarítási és facility management szolgáltatásokat Budapesten és Magyarország-szerte.",
  "foundingDate": "2010",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "HU",
    "addressLocality": "Budapest"
  },
  "employee": {
    "@type": "Person",
    "jobTitle": "Ügyvezető",
    "worksFor": {
      "@type": "Organization",
      "name": "West Style Kft."
    }
  }
}
</script>
```

**Before (current code):**
```html
<!-- No structured data present -->
```

**GitHub commit message:** `feat(seo): add Organization and Person schema to rolunk page`

---
---

## Fix: canonical — /referenciak

**Problem:** Canonical tag points to wrong domain `weststylekft.hu` instead of `weststylee.hu`.
**Where to add it:** Inside `<head>`, replace existing `<link rel="canonical">` tag

```html
<link rel="canonical" href="https://weststylee.hu/referenciak" />
```

**Before (current code):**
```html
<link rel="canonical" href="https://weststylekft.hu/referenciak" />
```

**GitHub commit message:** `fix(seo): correct canonical domain on referenciak page`

---

## Fix: meta_description — /referenciak

**Problem:** Meta description is too short at 82 characters and missing portfolio highlights.
**Where to add it:** Inside `<head>`, replace existing `<meta name="description">` tag

```html
<meta name="description" content="Tekintse meg a West Style Kft. referenciáit! Sikeres takarítási és facility management projektek irodákban, ipari létesítményekben és közintézményekben szerte Magyarországon." />
```

**Before (current code):**
```html
Check page source
```

**GitHub commit message:** `fix(seo): expand meta description on referenciak page`

---

## Fix: structured_data — /referenciak

**Problem:** No portfolio or project schema markup present on the references page.
**Where to add it:** Inside `<head>`, before closing `</head>` tag

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Referenciak – West Style Kft.",
  "url": "https://weststylee.hu/referenciak",
  "description": "West Style Kft. befejezett takarítási és facility management projektjeinek gyűjteménye.",
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "item": {
          "@type": "CreativeWork",
          "name": "Irodaház takarítási projekt",
          "description": "Rendszeres irodatakarítás nagyvállalati ügyfél számára Budapesten.",
          "provider": {
            "@type": "Organization",
            "name": "West Style Kft."
          }
        }
      },
      {
        "@type": "ListItem",
        "position": 2,
        "item": {
          "@type": "CreativeWork",
          "name": "Ipari létesítmény karbantartás",
          "description": "Komplex ipari takarítás és karbantartás gyártóüzem részére.",
          "provider": {
            "@type": "Organization",
            "name": "West Style Kft."
          }
        }
      }
    ]
  }
}
</script>
```

**Before (current code):**
```html
<!-- No structured data present -->
```

**GitHub commit message:** `feat(seo): add CreativeWork and CollectionPage schema to referenciak page`

---
---

## Fix: canonical — /kapcsolat

**Problem:** Canonical tag points to wrong domain `weststylekft.hu` instead of `weststylee.hu`.
**Where to add it:** Inside `<head>